docling>=2.0.0
sentence-transformers>=3.0.0
chromadb>=0.5.0
neo4j>=5.20.0
ollama>=0.3.0
streamlit>=1.38.0
kuzu>=0.11.0
pydantic>=2.0.0

# Pinned below streamlit's tested range: starlette>=1.0 changed
# GZipResponder's constructor signature in a way that breaks streamlit's
# gzip middleware (TypeError: missing 'thread_minimum_size') and silently
# kills page content while still serving the shell HTML. Unpin once
# streamlit ships a fix for newer starlette.
starlette==0.47.2
